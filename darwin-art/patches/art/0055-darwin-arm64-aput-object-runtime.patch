diff --git a/runtime/arch/arm64/quick_entrypoints_arm64.S b/runtime/arch/arm64/quick_entrypoints_arm64.S
--- a/runtime/arch/arm64/quick_entrypoints_arm64.S
+++ b/runtime/arch/arm64/quick_entrypoints_arm64.S
@@ -977,11 +977,14 @@
 #ifdef USE_READ_BARRIER
 #ifdef USE_BAKER_READ_BARRIER
 .macro BAKER_RB_CHECK_GRAY_BIT_AND_LOAD wDest, xObj, offset, gray_slow_path_label
-    ldr wIP0, [\xObj, #MIRROR_OBJECT_LOCK_WORD_OFFSET]
+    mov x16, #DARWIN_ART_REFERENCE_BASE
+    and x6, \xObj, #0xffffffff
+    add x6, x16, x6
+    ldr wIP0, [x6, #MIRROR_OBJECT_LOCK_WORD_OFFSET]
     tbnz wIP0, #LOCK_WORD_READ_BARRIER_STATE_SHIFT, \gray_slow_path_label
     // False dependency to avoid needing load/load fence.
-    add \xObj, \xObj, xIP0, lsr #32
-    ldr \wDest, [\xObj, #\offset]                      // Heap reference = 32b; zero-extends to xN.
+    add x6, x6, xIP0, lsr #32
+    ldr \wDest, [x6, #\offset]                         // Heap reference = 32b; zero-extends to xN.
     UNPOISON_HEAP_REF \wDest
 .endm
 
@@ -988,5 +991,8 @@
 .macro BAKER_RB_LOAD_AND_MARK wDest, xObj, offset, mark_function
-    ldr \wDest, [\xObj, #\offset]                      // Heap reference = 32b; zero-extends to xN.
+    mov x16, #DARWIN_ART_REFERENCE_BASE
+    and x6, \xObj, #0xffffffff
+    add x6, x16, x6
+    ldr \wDest, [x6, #\offset]                         // Heap reference = 32b; zero-extends to xN.
     UNPOISON_HEAP_REF \wDest
     // Save LR in a register preserved by `art_quick_read_barrier_mark_regNN`
     // and unused by the `art_quick_aput_obj`.
@@ -1034,26 +1034,35 @@
 #ifdef USE_READ_BARRIER
     cbnz wMR, .Laput_obj_gc_marking
 #endif  // USE_READ_BARRIER
-    ldr w3, [x0, #MIRROR_OBJECT_CLASS_OFFSET]          // Heap reference = 32b; zero-extends to x3.
+    mov x16, #DARWIN_ART_REFERENCE_BASE
+    add x5, x16, w0, uxtw
+    ldr w3, [x5, #MIRROR_OBJECT_CLASS_OFFSET]          // Heap reference = 32b; zero-extends to x3.
     UNPOISON_HEAP_REF w3
-    ldr w3, [x3, #MIRROR_CLASS_COMPONENT_TYPE_OFFSET]  // Heap reference = 32b; zero-extends to x3.
+    add x6, x16, w3, uxtw
+    ldr w3, [x6, #MIRROR_CLASS_COMPONENT_TYPE_OFFSET]  // Heap reference = 32b; zero-extends to x3.
     UNPOISON_HEAP_REF w3
-    ldr w4, [x2, #MIRROR_OBJECT_CLASS_OFFSET]          // Heap reference = 32b; zero-extends to x4.
+    add x7, x16, w2, uxtw
+    ldr w4, [x7, #MIRROR_OBJECT_CLASS_OFFSET]          // Heap reference = 32b; zero-extends to x4.
     UNPOISON_HEAP_REF w4
 #endif  // !defined(USE_READ_BARRIER) || defined(USE_BAKER_READ_BARRIER)
     cmp w3, w4  // value's type == array's component type - trivial assignability
     bne .Laput_obj_check_assignability
 .Laput_obj_store:
-    add x3, x0, #MIRROR_OBJECT_ARRAY_DATA_OFFSET
+    mov x16, #DARWIN_ART_REFERENCE_BASE
+    add x3, x16, w0, uxtw
+    add x3, x3, #MIRROR_OBJECT_ARRAY_DATA_OFFSET
     POISON_HEAP_REF w2
     str w2, [x3, x1, lsl #2]                           // Heap reference = 32b.
     ldr x3, [xSELF, #THREAD_CARD_TABLE_OFFSET]
+    add x0, x16, w0, uxtw
     lsr x0, x0, #CARD_TABLE_CARD_SHIFT
     strb w3, [x3, x0]
     ret
 
 .Laput_obj_null:
-    add x3, x0, #MIRROR_OBJECT_ARRAY_DATA_OFFSET
+    mov x16, #DARWIN_ART_REFERENCE_BASE
+    add x3, x16, w0, uxtw
+    add x3, x3, #MIRROR_OBJECT_ARRAY_DATA_OFFSET
     str w2, [x3, x1, lsl #2]                           // Heap reference = 32b.
     ret
 
@@ -1063,8 +1072,9 @@
     SAVE_TWO_REGS x2, xLR, 16
 
     // Call runtime code
-    mov x0, x3              // Heap reference, 32b, "uncompress" = do nothing, already zero-extended
-    mov x1, x4              // Heap reference, 32b, "uncompress" = do nothing, already zero-extended
+    mov x16, #DARWIN_ART_REFERENCE_BASE
+    add x0, x16, w3, uxtw   // Pass native component Class*.
+    add x1, x16, w4, uxtw   // Pass native value Class*.
     CALL_SYMBOL artIsAssignableFromCode
 
     // Check for exception
@@ -1075,10 +1085,13 @@
     RESTORE_TWO_REGS x2, xLR, 16
     RESTORE_TWO_REGS_DECREASE_FRAME x0, x1, 32
 
-    add x3, x0, #MIRROR_OBJECT_ARRAY_DATA_OFFSET
+    mov x16, #DARWIN_ART_REFERENCE_BASE
+    add x3, x16, w0, uxtw
+    add x3, x3, #MIRROR_OBJECT_ARRAY_DATA_OFFSET
     POISON_HEAP_REF w2
     str w2, [x3, x1, lsl #2]                           // Heap reference = 32b.
     ldr x3, [xSELF, #THREAD_CARD_TABLE_OFFSET]
+    add x0, x16, w0, uxtw
     lsr x0, x0, #CARD_TABLE_CARD_SHIFT
     strb w3, [x3, x0]
     ret
@@ -1092,7 +1105,9 @@
     CFI_REMEMBER_STATE
 #endif  // defined(USE_READ_BARRIER) && defined(USE_BAKER_READ_BARRIER)
     SETUP_SAVE_ALL_CALLEE_SAVES_FRAME
-    mov x1, x2                                         // Pass value.
+    mov x16, #DARWIN_ART_REFERENCE_BASE
+    add x0, x16, w0, uxtw                              // Pass native array.
+    add x1, x16, w2, uxtw                              // Pass native value.
     mov x2, xSELF                                      // Pass Thread::Current.
     CALL_SYMBOL artThrowArrayStoreException            // (Object*, Object*, Thread*).
     CALL_SYMBOL art_quick_do_long_jump                 // (Context*)
@@ -1314,7 +1329,11 @@
 #if ROSALLOC_SLOT_NEXT_OFFSET != MIRROR_OBJECT_CLASS_OFFSET
 #error "Class pointer needs to overwrite next pointer."
 #endif
     POISON_HEAP_REF w0
     str    w0, [x3, #MIRROR_OBJECT_CLASS_OFFSET]
+    // A native64 free-list link occupies both header words. AOSP low-4GiB
+    // pointers leave the upper word zero implicitly; make that invariant
+    // explicit before the object is published on Darwin.
+    str    wzr, [x3, #MIRROR_OBJECT_LOCK_WORD_OFFSET]
                                                               // Push the new object onto the thread
                                                               // local allocation stack and
