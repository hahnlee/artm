diff --git a/compiler/jni/quick/jni_compiler.cc b/compiler/jni/quick/jni_compiler.cc
--- a/compiler/jni/quick/jni_compiler.cc
+++ b/compiler/jni/quick/jni_compiler.cc
@@ -257,6 +257,7 @@
         __ Load(to_lock, mr_conv->CurrentParamStackOffset(), kObjectReferenceSize);
       }
     }
+    __ DecodeHeapReference(to_lock);
     __ CallFromThread(QUICK_ENTRYPOINT_OFFSET(kPointerSize, pJniLockObject));
   }
 
@@ -548,6 +549,7 @@
       // Pass the `this` argument from its spill slot.
       __ LoadStackReference(to_lock, mr_conv->CurrentParamStackOffset());
     }
+    __ DecodeHeapReference(to_lock);
     __ CallFromThread(QUICK_ENTRYPOINT_OFFSET(kPointerSize, pJniUnlockObject));
   }
 
diff --git a/compiler/utils/jni_macro_assembler.h b/compiler/utils/jni_macro_assembler.h
--- a/compiler/utils/jni_macro_assembler.h
+++ b/compiler/utils/jni_macro_assembler.h
@@ -142,6 +142,13 @@
   virtual void SignExtend(ManagedRegister mreg, size_t size) = 0;
 
   // Zero extension
   virtual void ZeroExtend(ManagedRegister mreg, size_t size) = 0;
+
+  // Decode a non-null managed heap reference before passing it to a runtime
+  // entrypoint that expects mirror::Object*. This is a no-op for AOSP's
+  // low-address heap representation.
+  virtual void DecodeHeapReference(ManagedRegister reg) {
+    UNUSED(reg);
+  }
 
   // Exploit fast access in managed code to Thread::Current()
   virtual void GetCurrentThread(ManagedRegister dest) = 0;
diff --git a/compiler/utils/arm64/jni_macro_assembler_arm64.h b/compiler/utils/arm64/jni_macro_assembler_arm64.h
--- a/compiler/utils/arm64/jni_macro_assembler_arm64.h
+++ b/compiler/utils/arm64/jni_macro_assembler_arm64.h
@@ -88,6 +88,8 @@
   // Zero extension.
   void ZeroExtend(ManagedRegister mreg, size_t size) override;
 
+  void DecodeHeapReference(ManagedRegister reg) override;
+
   // Exploit fast access in managed code to Thread::Current().
   void GetCurrentThread(ManagedRegister dest) override;
   void GetCurrentThread(FrameOffset dest_offset) override;
diff --git a/compiler/utils/arm64/jni_macro_assembler_arm64.cc b/compiler/utils/arm64/jni_macro_assembler_arm64.cc
--- a/compiler/utils/arm64/jni_macro_assembler_arm64.cc
+++ b/compiler/utils/arm64/jni_macro_assembler_arm64.cc
@@ -616,6 +616,14 @@
     ___ Uxth(reg_w(reg.AsWRegister()), reg_w(reg.AsWRegister()));
   }
 }
+
+void Arm64JNIMacroAssembler::DecodeHeapReference(ManagedRegister m_reg) {
+  Arm64ManagedRegister reg = m_reg.AsArm64();
+  CHECK(reg.IsWRegister());
+  ReferenceCodegenARM64<>::DecodeNonNull(asm_.GetVIXLAssembler(),
+                                         reg_x(reg.AsOverlappingXRegister()),
+                                         reg_w(reg.AsWRegister()));
+}
 
 void Arm64JNIMacroAssembler::VerifyObject(ManagedRegister /*src*/, bool /*could_be_null*/) {
   // TODO: not validating references.
