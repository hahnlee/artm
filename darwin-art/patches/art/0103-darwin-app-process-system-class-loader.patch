diff --git a/runtime/runtime.h b/runtime/runtime.h
index 5d1d12c..c8e1709 100644
--- a/runtime/runtime.h
+++ b/runtime/runtime.h
@@ -304,6 +304,12 @@ class Runtime {
   // Returns the system ClassLoader which represents the CLASSPATH.
   EXPORT jobject GetSystemClassLoader() const;
 
+  // app_process publishes its canonical PathClassLoader before application
+  // native threads attach. The caller transfers one owned global reference.
+  EXPORT jobject SetSystemClassLoaderForAppProcess(jobject class_loader) {
+    return std::exchange(system_class_loader_, class_loader);
+  }
+
   // Attaches the calling native thread to the runtime.
   EXPORT bool AttachCurrentThread(const char* thread_name,
                                   bool as_daemon,
diff --git a/runtime/runtime.cc b/runtime/runtime.cc
index b4a7e80..c532b1e 100644
--- a/runtime/runtime.cc
+++ b/runtime/runtime.cc
@@ -2578,6 +2578,11 @@ bool Runtime::AttachCurrentThread(const char* thread_name, bool as_daemon, jobjec
                                 create_peer,
                                 should_run_callbacks);
+  if (self != nullptr && system_class_loader_ != nullptr) {
+    // The Darwin app-process loader is published after Runtime::Start().
+    // Preserve Android's FindClass semantics for subsequently attached JNI threads.
+    self->SetClassLoaderOverride(system_class_loader_);
+  }
   // Run ThreadGroup.add to notify the group that this thread is now started.
   if (self != nullptr && create_peer && !IsAotCompiler()) {
     ScopedObjectAccess soa(self);
