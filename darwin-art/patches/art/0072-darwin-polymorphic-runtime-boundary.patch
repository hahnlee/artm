--- a/runtime/entrypoints/quick/quick_trampoline_entrypoints.cc
+++ b/runtime/entrypoints/quick/quick_trampoline_entrypoints.cc
@@ -2408,4 +2408,8 @@
 extern "C" uint64_t artInvokePolymorphic(mirror::Object* raw_receiver, Thread* self, ArtMethod** sp)
     REQUIRES_SHARED(Locks::mutator_lock_) {
+#if defined(__APPLE__)
+  raw_receiver = mirror::PtrCompression<false, mirror::Object>::Decompress(
+      static_cast<uint32_t>(reinterpret_cast<uintptr_t>(raw_receiver)));
+#endif
   ScopedQuickEntrypointChecks sqec(self);
   DCHECK(raw_receiver != nullptr);
@@ -2528,5 +2532,9 @@
-  return NanBoxResultIfNeeded(result.GetJ(), shorty[0]);
+#if defined(__APPLE__)
+  return DarwinCompressReferenceResultIfNeeded(NanBoxResultIfNeeded(result.GetJ(), shorty[0]), shorty[0]);
+#else
+  return NanBoxResultIfNeeded(result.GetJ(), shorty[0]);
+#endif
 }
 
 extern "C" uint64_t artInvokePolymorphicWithHiddenReceiver(mirror::Object* raw_receiver,
                                                            Thread* self,
@@ -2533,4 +2541,8 @@
                                                            ArtMethod** sp)
     REQUIRES_SHARED(Locks::mutator_lock_) {
+#if defined(__APPLE__)
+  raw_receiver = mirror::PtrCompression<false, mirror::Object>::Decompress(
+      static_cast<uint32_t>(reinterpret_cast<uintptr_t>(raw_receiver)));
+#endif
   ScopedQuickEntrypointChecks sqec(self);
   DCHECK(raw_receiver != nullptr);
@@ -2628,4 +2640,8 @@
-  return NanBoxResultIfNeeded(result.GetJ(), shorty[0]);
+#if defined(__APPLE__)
+  return DarwinCompressReferenceResultIfNeeded(NanBoxResultIfNeeded(result.GetJ(), shorty[0]), shorty[0]);
+#else
+  return NanBoxResultIfNeeded(result.GetJ(), shorty[0]);
+#endif
 }
 
 // Returns uint64_t representing raw bits from JValue.
--- a/runtime/method_handles.cc
+++ b/runtime/method_handles.cc
@@ -335,7 +335,9 @@
     // If both register locations contains the same value, the register probably holds a reference.
     // Note: As an optimization, non-moving collectors leave a stale reference value
     // in the references array even after the original vreg was overwritten to a non-reference.
-    if (src_value == reinterpret_cast<uintptr_t>(o.Ptr())) {
+    // Vregs contain compressed references, even when native pointers use a heap base.
+    if (src_value == mirror::CompressedReference<mirror::Object>::FromMirrorPtr(
+                         o.Ptr()).AsVRegValue()) {
       callee_frame->SetVRegReference(dst_reg, o);
     } else {
       callee_frame->SetVReg(dst_reg, src_value);
