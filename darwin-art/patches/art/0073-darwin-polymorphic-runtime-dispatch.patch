--- a/compiler/optimizing/intrinsics_arm64.cc
+++ b/compiler/optimizing/intrinsics_arm64.cc
@@ -17,1 +17,2 @@
-#include "intrinsics_arm64.h"
+#include "intrinsics_arm64.h"
+#include "art_reference_codegen_arm64.h"
@@ -6003,3 +6003,3 @@
-  locations->AddRegisterTemps(4);
-
-  if (!receiver_mh_loc.IsRegister()) {
+  locations->AddRegisterTemps(5);  // Native address temporary; HIR references stay compressed.
+
+  if (!receiver_mh_loc.IsRegister()) {
@@ -6017,1 +6017,1 @@
-      : WRegisterFrom(locations->GetTemp(5));
+      : WRegisterFrom(locations->GetTemp(6));
@@ -6032,2 +6032,5 @@
-  Register temp = WRegisterFrom(locations->GetTemp(1));
-  __ Ldr(temp, HeapOperand(method_handle.W(), mirror::MethodHandle::MethodTypeOffset()));
+  Register temp = WRegisterFrom(locations->GetTemp(1));
+  Register native_address = XRegisterFrom(locations->GetTemp(5));
+  ReferenceCodegenARM64<>::DecodeNonNull(masm, native_address, method_handle.W());
+  VLOG(jit) << "Darwin MethodHandle invokeExact intrinsic " << codegen_->GetGraph()->PrettyMethod() << " stack_handle=" << !receiver_mh_loc.IsRegister();
+  __ Ldr(temp, MemOperand(native_address, mirror::MethodHandle::MethodTypeOffset().Int32Value()));
@@ -6039,1 +6039,1 @@
-  __ Ldr(method, HeapOperand(method_handle.W(), mirror::MethodHandle::ArtFieldOrMethodOffset()));
+  __ Ldr(method, MemOperand(native_address, mirror::MethodHandle::ArtFieldOrMethodOffset().Int32Value()));
@@ -6045,2 +6045,2 @@
-  __ Ldr(method_handle_kind,
-         HeapOperand(method_handle.W(), mirror::MethodHandle::HandleKindOffset()));
+  __ Ldr(method_handle_kind,
+         MemOperand(native_address, mirror::MethodHandle::HandleKindOffset().Int32Value()));
@@ -6050,1 +6050,1 @@
-  __ Ldr(method, HeapOperand(method_handle.W(), mirror::MethodHandleImpl::TargetOffset()));
+  __ Ldr(method, MemOperand(native_address, mirror::MethodHandleImpl::TargetOffset().Int32Value()));
@@ -6060,1 +6060,2 @@
-    __ Cbz(receiver, slow_path->GetEntryLabel());
+    __ Cbz(receiver, slow_path->GetEntryLabel());
+    ReferenceCodegenARM64<>::DecodeNonNull(masm, native_address, receiver.W());
@@ -6078,1 +6078,1 @@
-    __ Ldr(receiver_class, HeapOperand(receiver.W(), mirror::Object::ClassOffset().Int32Value()));
+    __ Ldr(receiver_class, MemOperand(native_address, mirror::Object::ClassOffset().Int32Value()));
@@ -6090,1 +6090,2 @@
-    __ Add(receiver_class.X(), receiver_class.X(), vtable_offset);
+    ReferenceCodegenARM64<>::DecodeNonNull(masm, receiver_class.X(), receiver_class.W());
+    __ Add(receiver_class.X(), receiver_class.X(), vtable_offset);
@@ -6133,1 +6133,1 @@
-    __ Ldr(method.W(), HeapOperand(receiver.W(), mirror::Object::ClassOffset()));
+    __ Ldr(method.W(), MemOperand(native_address, mirror::Object::ClassOffset().Int32Value()));
@@ -6136,1 +6136,2 @@
-    __ Ldr(method, MemOperand(method, mirror::Class::ImtPtrOffset(PointerSize::k64).Int32Value()));
+    ReferenceCodegenARM64<>::DecodeNonNull(masm, method, method.W());
+    __ Ldr(method, MemOperand(method, mirror::Class::ImtPtrOffset(PointerSize::k64).Int32Value()));
--- a/compiler/optimizing/code_generator_arm64.cc
+++ b/compiler/optimizing/code_generator_arm64.cc
@@ -294,2 +294,3 @@
-    arm64_codegen->InvokeRuntime(kQuickResolveMethodType, instruction_, this);
-    CheckEntrypointTypes<kQuickResolveMethodType, void*, uint32_t>();
+    arm64_codegen->InvokeRuntime(kQuickResolveMethodType, instruction_, this);
+    ReferenceCodegenARM64<>::Encode(arm64_codegen->GetVIXLAssembler(), x0, x0);
+    CheckEntrypointTypes<kQuickResolveMethodType, void*, uint32_t>();
@@ -5936,2 +5936,3 @@
-      codegen_->GenerateLoadMethodTypeRuntimeCall(load);
-      break;
+      codegen_->GenerateLoadMethodTypeRuntimeCall(load);
+      ReferenceCodegenARM64<>::Encode(GetVIXLAssembler(), x0, x0);
+      break;
