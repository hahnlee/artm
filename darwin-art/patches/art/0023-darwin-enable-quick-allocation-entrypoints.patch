diff --git a/runtime/entrypoints/quick/quick_alloc_entrypoints.cc b/runtime/entrypoints/quick/quick_alloc_entrypoints.cc
--- a/runtime/entrypoints/quick/quick_alloc_entrypoints.cc
+++ b/runtime/entrypoints/quick/quick_alloc_entrypoints.cc
@@ -197,7 +197,7 @@ void SetQuickAllocEntryPoints##suffix(QuickEntryPoints* qpoints, bool instrumented) { \
 }
 
 // Generate the entrypoint functions.
-#if !defined(__APPLE__) || !defined(__LP64__)
+#if 1  // Darwin arm64 assembly symbols are exported by the Mach-O ART port.
 GENERATE_ENTRYPOINTS(_dlmalloc)
 GENERATE_ENTRYPOINTS(_rosalloc)
 GENERATE_ENTRYPOINTS(_bump_pointer)
@@ -215,7 +215,7 @@ void SetQuickAllocEntryPointsInstrumented(bool instrumented) {
 }
 
 void ResetQuickAllocEntryPoints(QuickEntryPoints* qpoints) {
-#if !defined(__APPLE__) || !defined(__LP64__)
+#if 1  // Darwin arm64 assembly symbols are exported by the Mach-O ART port.
   switch (entry_points_allocator) {
     case gc::kAllocatorTypeDlMalloc: {
       SetQuickAllocEntryPoints_dlmalloc(qpoints, entry_points_instrumented);
