diff --git a/libs/binder/ndk/include_cpp/android/binder_auto_utils.h b/libs/binder/ndk/include_cpp/android/binder_auto_utils.h
index 8c62924..45b3841 100644
--- a/libs/binder/ndk/include_cpp/android/binder_auto_utils.h
+++ b/libs/binder/ndk/include_cpp/android/binder_auto_utils.h
@@ -91,7 +91,7 @@ class SpAIBinder {
         AIBinder* old = *const_cast<AIBinder* volatile*>(&mBinder);
         if (old != nullptr) AIBinder_decStrong(old);
         if (old != *const_cast<AIBinder* volatile*>(&mBinder)) {
-            __assert(__FILE__, __LINE__, "Race detected.");
+            assert(false && "Race detected.");
         }
         mBinder = binder;
     }
diff --git a/libs/binder/ndk/include_cpp/android/binder_interface_utils.h b/libs/binder/ndk/include_cpp/android/binder_interface_utils.h
index c6518d8..db7a755 100644
--- a/libs/binder/ndk/include_cpp/android/binder_interface_utils.h
+++ b/libs/binder/ndk/include_cpp/android/binder_interface_utils.h
@@ -64,13 +64,13 @@ class SharedRefBase {
     SharedRefBase() {}
     virtual ~SharedRefBase() {
         std::call_once(mFlagThis, [&]() {
-            __assert(__FILE__, __LINE__, "SharedRefBase: no ref created during lifetime");
+            assert(false && "SharedRefBase: no ref created during lifetime");
         });
 
         if (ref() != nullptr) {
-            __assert(__FILE__, __LINE__,
-                     "SharedRefBase: destructed but still able to lock weak_ptr. Is this object "
-                     "double-owned?");
+            assert(false &&
+                   "SharedRefBase: destructed but still able to lock weak_ptr. Is this object "
+                   "double-owned?");
         }
     }
 
diff --git a/libs/gui/include/gui/LayerState.h b/libs/gui/include/gui/LayerState.h
index e2d27ac..03db46b 100644
--- a/libs/gui/include/gui/LayerState.h
+++ b/libs/gui/include/gui/LayerState.h
@@ -28,6 +28,7 @@
 #include <android/gui/TrustedPresentationThresholds.h>
 #include <android/native_window.h>
 #include <gui/DisplayLuts.h>
+#include <gui/HdrMetadata.h>
 #include <gui/IGraphicBufferProducer.h>
 #include <gui/ITransactionCompletedListener.h>
 #include <math/mat4.h>
diff --git a/services/surfaceflinger/FrontEnd/LayerLog.h b/services/surfaceflinger/FrontEnd/LayerLog.h
index 3845dfe..7ecadde 100644
--- a/services/surfaceflinger/FrontEnd/LayerLog.h
+++ b/services/surfaceflinger/FrontEnd/LayerLog.h
@@ -16,7 +16,11 @@
 
 #pragma once
 
+#if defined(__APPLE__)
+#include <utils/Log.h>
+#else
 #include "Tracing/TransactionTracing.h"
+#endif
 
 // Uncomment to trace layer updates for a single layer
 // #define LOG_LAYER 1
@@ -30,6 +34,12 @@
 
 #define LLOGD(LAYER_ID, x, ...) ALOGD("[%d] %s " x, (LAYER_ID), __func__, ##__VA_ARGS__);
 
+#if defined(__APPLE__)
+#define LLOG_ALWAYS_FATAL_WITH_TRACE(...) LOG_ALWAYS_FATAL(##__VA_ARGS__)
+#define LLOG_ALWAYS_FATAL_WITH_TRACE_IF(cond, ...) LOG_ALWAYS_FATAL_IF(cond, ##__VA_ARGS__)
+#undef SFTRACE_NAME
+#define SFTRACE_NAME(...) ((void)0)
+#else
 #define LLOG_ALWAYS_FATAL_WITH_TRACE(...)                                               \
     do {                                                                                \
         TransactionTraceWriter::getInstance().invoke(__func__, /* overwrite= */ false); \
@@ -42,4 +52,5 @@
             TransactionTraceWriter::getInstance().invoke(__func__, /* overwrite= */ false); \
         }                                                                                   \
         LOG_ALWAYS_FATAL_IF(cond, ##__VA_ARGS__);                                           \
-    } while (false)
\ No newline at end of file
+    } while (false)
+#endif
diff --git a/libs/binder/include/binder/Delegate.h b/libs/binder/include/binder/Delegate.h
index 7eab000..a6aaf01 100644
--- a/libs/binder/include/binder/Delegate.h
+++ b/libs/binder/include/binder/Delegate.h
@@ -65,8 +65,8 @@ sp<T> delegate(const sp<T>& binder) {
     if (T::asBinder(binder)->findObject(isDelegatorId)) {
         if (T::asBinder(binder)->findObject(hasDelegatorId)) {
-            __assert(__FILE__, __LINE__,
-                     "This binder has a delegator and is also delegator itself! This is "
-                     "likely an unintended mixing of binders.");
+            assert(false &&
+                   "This binder has a delegator and is also delegator itself! This is "
+                   "likely an unintended mixing of binders.");
             return nullptr;
         }
         // unwrap the delegator
diff --git a/services/surfaceflinger/FrontEnd/RequestedLayerState.h b/services/surfaceflinger/FrontEnd/RequestedLayerState.h
index cdb2831..d5927a1 100644
--- a/services/surfaceflinger/FrontEnd/RequestedLayerState.h
+++ b/services/surfaceflinger/FrontEnd/RequestedLayerState.h
@@ -20,7 +20,26 @@
 #include <ftl/flags.h>
 #include <gui/LayerState.h>
 #include <renderengine/ExternalTexture.h>
+#if defined(__APPLE__)
+namespace android::scheduler {
+class LayerInfo {
+public:
+    enum class FrameRateSelectionStrategy { Propagate, OverrideChildren, Self };
+    enum class DarwinVoteType : uint8_t { Default, ftl_last = Default };
+    enum class DarwinCategory : uint8_t { Default, ftl_last = Default };
+    struct FrameRate {
+        struct Vote {
+            float rate = 0;
+            DarwinVoteType type = DarwinVoteType::Default;
+        } vote;
+        DarwinCategory category = DarwinCategory::Default;
+        bool isValid() const { return false; }
+    };
+};
+} // namespace android::scheduler
+#else
 #include "Scheduler/LayerInfo.h"
+#endif
 
 #include "LayerCreationArgs.h"
 #include "QueuedTransactionState.h"
diff --git a/services/surfaceflinger/FrontEnd/RequestedLayerState.cpp b/services/surfaceflinger/FrontEnd/RequestedLayerState.cpp
index 466fb36..af8ace1 100644
--- a/services/surfaceflinger/FrontEnd/RequestedLayerState.cpp
+++ b/services/surfaceflinger/FrontEnd/RequestedLayerState.cpp
@@ -24,8 +24,10 @@
 #include <private/android_filesystem_config.h>
 #include <sys/types.h>
 
+#if !defined(__APPLE__)
 #include <scheduler/Fps.h>
+#include "Layer.h"
+#endif
 
-#include "Layer.h"
 #include "LayerCreationArgs.h"
 #include "LayerLog.h"
@@ -32,6 +34,31 @@
 #include "RequestedLayerState.h"
 
+#if defined(__APPLE__)
+namespace android::surfaceflinger::frontend {
+struct DarwinLayerSnapshot {
+    static bool isOpaqueFormat(PixelFormat format) {
+        if (format >= 0x100 && format <= 0x1ff) return true;
+        switch (format) {
+            case PIXEL_FORMAT_RGBA_8888:
+            case PIXEL_FORMAT_BGRA_8888:
+            case PIXEL_FORMAT_RGBA_FP16:
+            case PIXEL_FORMAT_RGBA_1010102:
+            case PIXEL_FORMAT_R_8:
+                return false;
+            default:
+                return true;
+        }
+    }
+    static bool isTransformValid(const ui::Transform& transform) {
+        const float determinant = transform.det();
+        return determinant != 0 && !std::isinf(determinant) &&
+                !std::isnan(determinant);
+    }
+};
+} // namespace android::surfaceflinger::frontend
+#define LayerSnapshot DarwinLayerSnapshot
+#endif
 
 namespace android::surfaceflinger::frontend {
 using ftl::Flags;
 using namespace ftl::flag_operators;
@@ -123,5 +150,9 @@ RequestedLayerState::RequestedLayerState(const LayerCreationArgs& args)
     colorSpaceAgnostic = false;
+#if defined(__APPLE__)
+    frameRateSelectionPriority = -1;
+#else
     frameRateSelectionPriority = Layer::PRIORITY_UNSET;
+#endif
     shadowRadius = 0.f;
     fixedTransformHint = ui::Transform::ROT_INVALID;
     destinationFrame.makeInvalid();
@@ -128,11 +159,20 @@ RequestedLayerState::RequestedLayerState(const LayerCreationArgs& args)
     trustedOverlay = gui::TrustedOverlay::UNSET;
     dropInputMode = gui::DropInputMode::NONE;
     dimmingEnabled = true;
+#if defined(__APPLE__)
+    defaultFrameRateCompatibility = 0;
+    frameRateCategory = 0;
+#else
     defaultFrameRateCompatibility = static_cast<int8_t>(scheduler::FrameRateCompatibility::Default);
     frameRateCategory = static_cast<int8_t>(FrameRateCategory::Default);
+#endif
     frameRateCategorySmoothSwitchOnly = false;
+#if defined(__APPLE__)
+    frameRateSelectionStrategy = 0;
+#else
     frameRateSelectionStrategy =
             static_cast<int8_t>(scheduler::LayerInfo::FrameRateSelectionStrategy::Propagate);
+#endif
     dataspace = ui::Dataspace::V0_SRGB;
     gameMode = gui::GameMode::Unsupported;
     requestedFrameRate = {};
@@ -219,13 +259,15 @@ void RequestedLayerState::merge(const ResolvedComposerState& resolvedComposerSta
             if ((barrierProducerId > bufferData->producerId) ||
                 ((barrierProducerId == bufferData->producerId) &&
                  (barrierFrameNumber > bufferData->frameNumber))) {
                 ALOGE("Out of order buffers detected for %s producedId=%d frameNumber=%" PRIu64
                       " -> producedId=%d frameNumber=%" PRIu64,
                       getDebugString().c_str(), barrierProducerId, barrierFrameNumber,
                       bufferData->producerId, frameNumber);
+#if !defined(__APPLE__)
                 TransactionTraceWriter::getInstance().invoke("out_of_order_buffers_",
                                                              /*overwrite=*/false);
+#endif
             }
 
             barrierProducerId = std::max(bufferData->producerId, barrierProducerId);
             barrierFrameNumber = std::max(bufferData->frameNumber, barrierFrameNumber);
@@ -338,6 +380,7 @@ void RequestedLayerState::merge(const ResolvedComposerState& resolvedComposerSta
         }
         changes |= RequestedLayerState::Changes::Metadata;
     }
+#if !defined(__APPLE__)
     if (clientState.what & layer_state_t::eFrameRateChanged) {
         const auto compatibility =
                 Layer::FrameRate::convertCompatibility(clientState.frameRateCompatibility);
@@ -354,6 +397,7 @@ void RequestedLayerState::merge(const ResolvedComposerState& resolvedComposerSta
         requestedFrameRate.category = category;
         changes |= RequestedLayerState::Changes::FrameRate;
     }
+#endif
 
     if (clientState.what & layer_state_t::eClientDrawnCornerRadiusChanged) {
         clientDrawnCornerRadius = clientState.clientDrawnCornerRadius;
diff --git a/services/surfaceflinger/FrontEnd/LayerHierarchy.cpp b/services/surfaceflinger/FrontEnd/LayerHierarchy.cpp
index 6884055..dc0572b 100644
--- a/services/surfaceflinger/FrontEnd/LayerHierarchy.cpp
+++ b/services/surfaceflinger/FrontEnd/LayerHierarchy.cpp
@@ -428,8 +428,10 @@ void LayerHierarchyBuilder::update(LayerLifecycleManager& layerLifecycleManager)
     bool hasRelZLoop = mRoot.hasRelZLoop(invalidRelativeRoot);
     while (hasRelZLoop) {
         SFTRACE_NAME("FixRelZLoop");
+#if !defined(__APPLE__)
         TransactionTraceWriter::getInstance().invoke("relz_loop_detected",
                                                      /*overwrite=*/false);
+#endif
         layerLifecycleManager.fixRelativeZLoop(invalidRelativeRoot);
         // reinitialize the hierarchy with the updated layer data
         init(layerLifecycleManager.getLayers());
diff --git a/services/surfaceflinger/FrontEnd/LayerCreationArgs.cpp b/services/surfaceflinger/FrontEnd/LayerCreationArgs.cpp
index 198bacf..e45dd8c 100644
--- a/services/surfaceflinger/FrontEnd/LayerCreationArgs.cpp
+++ b/services/surfaceflinger/FrontEnd/LayerCreationArgs.cpp
@@ -38,8 +38,14 @@ LayerCreationArgs::LayerCreationArgs(SurfaceFlinger* flinger, sp<Client> client,
         flags(flags),
         metadata(std::move(metadataArg)) {
+#if defined(__APPLE__)
+    ownerPid = 1;
+    ownerUid = 1;
+#else
     IPCThreadState* ipc = IPCThreadState::self();
     ownerPid = ipc->getCallingPid();
     uid_t callingUid = ipc->getCallingUid();
+#endif
+#if !defined(__APPLE__)
     metadata.setInt32(gui::METADATA_CALLING_UID, static_cast<int32_t>(callingUid));
     ownerUid = callingUid;
     if (ownerUid == AID_GRAPHICS || ownerUid == AID_SYSTEM) {
@@ -50,5 +56,6 @@ LayerCreationArgs::LayerCreationArgs(SurfaceFlinger* flinger, sp<Client> client,
                 metadata.getInt32(gui::METADATA_OWNER_UID, static_cast<int32_t>(ownerUid)));
     }
+#endif
 
     if (internalLayer) {
         sequence = id.value_or(getInternalLayerId(sInternalSequence++));
diff --git a/services/surfaceflinger/FrontEnd/LayerLifecycleManager.cpp b/services/surfaceflinger/FrontEnd/LayerLifecycleManager.cpp
index 2b3adbd..763a39e 100644
--- a/services/surfaceflinger/FrontEnd/LayerLifecycleManager.cpp
+++ b/services/surfaceflinger/FrontEnd/LayerLifecycleManager.cpp
@@ -73,8 +73,13 @@ void LayerLifecycleManager::addLayers(
         } else {
             // Check if we are mirroring a single layer, and if so add it to the list of children
             // to be mirrored.
+#if defined(__APPLE__)
+            constexpr bool detachedMirror = false;
+#else
+            const bool detachedMirror = FlagManager::getInstance().detached_mirror();
+#endif
             layer.layerIdToMirror = linkLayer(layer.layerIdToMirror, layer.id);
-            if (!FlagManager::getInstance().detached_mirror()) {
+            if (!detachedMirror) {
                 if (layer.layerIdToMirror != UNASSIGNED_LAYER_ID) {
                     layer.mirrorIds.emplace_back(layer.layerIdToMirror);
                 }
diff --git a/services/surfaceflinger/FrontEnd/LayerHierarchy.cpp b/services/surfaceflinger/FrontEnd/LayerHierarchy.cpp
index dc0572b..cd34182 100644
--- a/services/surfaceflinger/FrontEnd/LayerHierarchy.cpp
+++ b/services/surfaceflinger/FrontEnd/LayerHierarchy.cpp
@@ -303,7 +303,12 @@ void LayerHierarchyBuilder::onLayerAdded(RequestedLayerState* layer) {
         LayerHierarchy* mirror = getHierarchyFromId(mirrorId);
         hierarchy->addChild(mirror, LayerHierarchy::Variant::Mirror);
     }
-    if (FlagManager::getInstance().detached_mirror()) {
+#if defined(__APPLE__)
+    constexpr bool detachedMirror = false;
+#else
+    const bool detachedMirror = FlagManager::getInstance().detached_mirror();
+#endif
+    if (detachedMirror) {
         if (layer->layerIdToMirror != UNASSIGNED_LAYER_ID) {
             LayerHierarchy* mirror = getHierarchyFromId(layer->layerIdToMirror);
             hierarchy->addChild(mirror, LayerHierarchy::Variant::Detached_Mirror);
@@ -355,7 +360,12 @@ void LayerHierarchyBuilder::updateMirrorLayer(RequestedLayerState* layer) {
     for (uint32_t mirrorId : layer->mirrorIds) {
         hierarchy->addChild(getHierarchyFromId(mirrorId), LayerHierarchy::Variant::Mirror);
     }
-    if (FlagManager::getInstance().detached_mirror()) {
+#if defined(__APPLE__)
+    constexpr bool detachedMirror = false;
+#else
+    const bool detachedMirror = FlagManager::getInstance().detached_mirror();
+#endif
+    if (detachedMirror) {
         if (layer->layerIdToMirror != UNASSIGNED_LAYER_ID) {
             hierarchy->addChild(getHierarchyFromId(layer->layerIdToMirror),
                                 LayerHierarchy::Variant::Detached_Mirror);
diff --git a/libs/gui/LayerState.cpp b/libs/gui/LayerState.cpp
index 6ee14a8..bd69fb1 100644
--- a/libs/gui/LayerState.cpp
+++ b/libs/gui/LayerState.cpp
@@ -859,9 +859,15 @@ uint64_t layer_state_t::diff(const layer_state_t& other) const {
         diff |= eRelativeLayerChanged;
         diff &= ~eLayerChanged;
     }
+#if defined(__APPLE__)
     if (other.what & eReparent &&
+        mNotDefCmpState.parentSurfaceControlForChild.get() !=
+                other.mNotDefCmpState.parentSurfaceControlForChild.get()) {
+#else
+    if (other.what & eReparent &&
         !SurfaceControl::isSameSurface(mNotDefCmpState.parentSurfaceControlForChild,
                                        other.mNotDefCmpState.parentSurfaceControlForChild)) {
+#endif
         diff |= eReparent;
     }
     CHECK_DIFF(diff, eBufferTransformChanged, other, bufferTransform);
@@ -1035,12 +1041,17 @@ status_t BufferData::writeToParcel(Parcel* output) const {
 status_t BufferData::writeToParcel(Parcel* output) const {
     SAFE_PARCEL(output->writeInt32, flags.get());
 
+#if defined(__APPLE__)
+    if (buffer) return INVALID_OPERATION;
+    SAFE_PARCEL(output->writeBool, false);
+#else
     if (buffer) {
         SAFE_PARCEL(output->writeBool, true);
         SAFE_PARCEL(output->write, *buffer);
     } else {
         SAFE_PARCEL(output->writeBool, false);
     }
+#endif
 
     if (acquireFence) {
         SAFE_PARCEL(output->writeBool, true);
@@ -1070,10 +1081,14 @@ status_t BufferData::readFromParcel(const Parcel* input) {
 
     bool tmpBool = false;
     SAFE_PARCEL(input->readBool, &tmpBool);
+#if defined(__APPLE__)
+    if (tmpBool) return INVALID_OPERATION;
+#else
     if (tmpBool) {
         buffer = sp<GraphicBuffer>::make();
         SAFE_PARCEL(input->read, *buffer);
     }
+#endif
 
     SAFE_PARCEL(input->readBool, &tmpBool);
     if (tmpBool) {
