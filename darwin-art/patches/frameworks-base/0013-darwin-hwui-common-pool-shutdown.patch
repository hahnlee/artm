--- a/thread/CommonPool.h
+++ b/thread/CommonPool.h
@@ -108,10 +108,7 @@
     static CommonPool& instance();
 
     CommonPool();
-    ~CommonPool() {
-        mIsStopping = true;
-        mCondition.notify_all();
-    }
+    ~CommonPool();
 
     void enqueue(Task&&);
     void doWaitForIdle();
@@ -119,6 +116,7 @@
     void workerLoop();
 
     std::vector<int> mWorkerThreadIds;
+    std::vector<std::thread> mWorkers;
 
     std::mutex mLock;
     std::condition_variable mCondition;
--- a/thread/CommonPool.cpp
+++ b/thread/CommonPool.cpp
@@ -31,13 +31,13 @@
     std::vector<int> tids(THREAD_COUNT);
     std::vector<std::condition_variable> tidConditionVars(THREAD_COUNT);
 
-    // Create 2 workers
+    mWorkers.reserve(THREAD_COUNT);
+    // Create 2 workers owned by the pool so shutdown can join them.
     for (int i = 0; i < THREAD_COUNT; i++) {
-        std::thread worker([pool, i, &mLock, &tids, &tidConditionVars] {
+        mWorkers.emplace_back([pool, i, &mLock, &tids, &tidConditionVars] {
             pool->setupThread(i, mLock, tids, tidConditionVars);
             pool->workerLoop();
         });
-        worker.detach();
     }
     {
         std::unique_lock lock(mLock);
@@ -52,6 +52,14 @@
     }
 }
 
+CommonPool::~CommonPool() {
+    mIsStopping = true;
+    mCondition.notify_all();
+    for (auto& worker : mWorkers) {
+        if (worker.joinable()) worker.join();
+    }
+}
+
 CommonPool& CommonPool::instance() {
     static CommonPool pool;
     return pool;
@@ -68,6 +76,7 @@
 void CommonPool::enqueue(Task&& task) {
     std::unique_lock lock(mLock);
     while (!mWorkQueue.hasSpace()) {
+        if (mIsStopping) return;
         lock.unlock();
         usleep(100);
         lock.lock();
@@ -80,12 +89,13 @@
 
 void CommonPool::workerLoop() {
     std::unique_lock lock(mLock);
-    while (!mIsStopping) {
+    while (true) {
         if (!mWorkQueue.hasWork()) {
             mWaitingThreads++;
-            mCondition.wait(lock);
+            mCondition.wait(lock, [this] { return mIsStopping || mWorkQueue.hasWork(); });
             mWaitingThreads--;
         }
+        if (mIsStopping && !mWorkQueue.hasWork()) return;
         // Need to double-check that work is still available now that we have the lock
         // It may have already been grabbed by a different thread
         while (mWorkQueue.hasWork()) {
@@ -94,5 +104,6 @@
             work();
             lock.lock();
         }
+        if (mIsStopping) return;
     }
 }
