diff --git a/renderthread/EglManager.cpp b/renderthread/EglManager.cpp
index 2fc769ac1..3af4f980d 100644
--- a/renderthread/EglManager.cpp
+++ b/renderthread/EglManager.cpp
@@ -518,7 +518,8 @@ Result<EGLSurface, EGLint> EglManager::createSurface(EGLNativeWindowType window,
         // This relies on knowing that EGL will not re-set the dataspace after the call to
         // eglCreateWindowSurface. Since the handling of the colorspace extension is largely
         // implemented in libEGL in the platform, we can safely assume this is the case
-        int32_t err = ANativeWindow_setBuffersDataSpace(window, P3_XRB);
+        int32_t err = ANativeWindow_setBuffersDataSpace(
+                reinterpret_cast<ANativeWindow*>(window), P3_XRB);
         LOG_ALWAYS_FATAL_IF(err, "Failed to ANativeWindow_setBuffersDataSpace %d", err);
     }
 
diff --git a/renderthread/CanvasContext.cpp b/renderthread/CanvasContext.cpp
index 03b91a540..b2447842a 100644
--- a/renderthread/CanvasContext.cpp
+++ b/renderthread/CanvasContext.cpp
@@ -44,7 +44,9 @@
 #include "pipeline/skia/SkiaCpuPipeline.h"
 #include "pipeline/skia/SkiaGpuPipeline.h"
 #include "pipeline/skia/SkiaOpenGLPipeline.h"
+#if !defined(__APPLE__)
 #include "pipeline/skia/SkiaVulkanPipeline.h"
+#endif
 #include "thread/CommonPool.h"
 #include "utils/GLUtils.h"
 #include "utils/TimeUtils.h"
@@ -86,10 +88,12 @@ CanvasContext* CanvasContext::create(RenderThread& thread, bool translucent,
                                      std::make_unique<skiapipeline::SkiaOpenGLPipeline>(thread),
                                      uiThreadId, pid_t renderThreadId);
+#if !defined(__APPLE__)
         case RenderPipelineType::SkiaVulkan:
             return new CanvasContext(thread, translucent, rootRenderNode, contextFactory,
                                      std::make_unique<skiapipeline::SkiaVulkanPipeline>(thread),
                                      uiThreadId, renderThreadId);
-#ifndef __ANDROID__
+#endif
+#if !defined(__ANDROID__) && !defined(__APPLE__)
         case RenderPipelineType::SkiaCpu:
             return new CanvasContext(thread, translucent, rootRenderNode, contextFactory,
                                      std::make_unique<skiapipeline::SkiaCpuPipeline>(thread),
@@ -110,9 +114,11 @@ void CanvasContext::invokeFunctor(const RenderThread& thread, Functor* functor) {
             skiapipeline::SkiaOpenGLPipeline::invokeFunctor(thread, functor);
             break;
+#if !defined(__APPLE__)
         case RenderPipelineType::SkiaVulkan:
             skiapipeline::SkiaVulkanPipeline::invokeFunctor(thread, functor);
             break;
+#endif
         default:
             LOG_ALWAYS_FATAL("canvas context type %d not supported", (int32_t)renderType);
             break;
     }
diff --git a/DeferredLayerUpdater.cpp b/DeferredLayerUpdater.cpp
index bb5814f92..a75837f52 100644
--- a/DeferredLayerUpdater.cpp
+++ b/DeferredLayerUpdater.cpp
@@ -19,5 +19,7 @@
 #include <GLES2/gl2ext.h>
 
 // TODO: Use public SurfaceTexture APIs once available and include public NDK header file instead.
+#if !defined(__APPLE__)
 #include <statslog_hwui.h>
+#endif
 #include <surfacetexture/surface_texture_platform.h>
@@ -32,6 +34,8 @@
 #include "renderstate/RenderState.h"
 #include "renderthread/EglManager.h"
 #include "renderthread/RenderThread.h"
+#if !defined(__APPLE__)
 #include "renderthread/VulkanManager.h"
+#endif
 
 using namespace android::uirenderer::renderthread;
@@ -55,6 +59,7 @@ DeferredLayerUpdater::~DeferredLayerUpdater() {
     mRenderState.removeContextCallback(this);
     destroyLayer();
     if (mFirstTimeForDataspace > std::chrono::steady_clock::time_point::min()) {
+#if !defined(__APPLE__)
         auto currentTime = std::chrono::steady_clock::now();
         stats_write(stats::TEXTURE_VIEW_EVENT, static_cast<int32_t>(getuid()),
                     static_cast<int64_t>(std::chrono::duration_cast<std::chrono::milliseconds>(
@@ -62,4 +67,5 @@ DeferredLayerUpdater::~DeferredLayerUpdater() {
                                                  .count()),
                     mDataspace);
+#endif
     }
 }
@@ -112,6 +118,10 @@ status_t DeferredLayerUpdater::createReleaseFence(bool useFenceSync, EGLSyncKHR*
         *display = eglManager.eglDisplay();
         err = eglManager.createReleaseFence(useFenceSync, eglFence, releaseFence);
     } else {
+#if defined(__APPLE__)
+        LOG_ALWAYS_FATAL("Vulkan TextureView fences are unavailable on Darwin");
+        err = INVALID_OPERATION;
+#else
         int previousSlot = dlu->mCurrentSlot;
         if (previousSlot != -1) {
             dlu->mImageSlots[previousSlot].releaseQueueOwnership(
@@ -121,5 +131,6 @@ status_t DeferredLayerUpdater::createReleaseFence(bool useFenceSync, EGLSyncKHR*
         err = renderState.getRenderThread().vulkanManager().createReleaseFence(
                 releaseFence, renderState.getRenderThread().getGrContext());
+#endif
     }
     return err;
 }
@@ -133,8 +144,13 @@ status_t DeferredLayerUpdater::fenceWait(int fence, void* handle) {
     if (Properties::getRenderPipelineType() == RenderPipelineType::SkiaGL) {
         err = renderState.getRenderThread().eglManager().fenceWait(fence);
     } else {
+#if defined(__APPLE__)
+        LOG_ALWAYS_FATAL("Vulkan TextureView fences are unavailable on Darwin");
+        err = INVALID_OPERATION;
+#else
         err = renderState.getRenderThread().vulkanManager().fenceWait(
                 fence, renderState.getRenderThread().getGrContext());
+#endif
     }
     return err;
 }
@@ -200,11 +216,13 @@ void DeferredLayerUpdater::apply() {
                     auto currentTime = std::chrono::steady_clock::now();
                     if (mFirstTimeForDataspace > std::chrono::steady_clock::time_point::min()) {
+#if !defined(__APPLE__)
                         stats_write(stats::TEXTURE_VIEW_EVENT, static_cast<int32_t>(getuid()),
                                     static_cast<int64_t>(
                                             std::chrono::duration_cast<std::chrono::milliseconds>(
                                                     currentTime - mFirstTimeForDataspace)
                                                     .count()),
                                     mDataspace);
+#endif
                     }
                     mFirstTimeForDataspace = currentTime;
                     mDataspace = dataspace;
diff --git a/AutoBackendTextureRelease.cpp b/AutoBackendTextureRelease.cpp
index c646c00b5..0388ade6a 100644
--- a/AutoBackendTextureRelease.cpp
+++ b/AutoBackendTextureRelease.cpp
@@ -21,6 +21,8 @@
 #include <include/gpu/ganesh/GrBackendSurface.h>
 #include <include/gpu/ganesh/GrDirectContext.h>
 #include <include/gpu/ganesh/SkImageGanesh.h>
+#if !defined(__APPLE__)
 #include <include/gpu/vk/VulkanMutableTextureState.h>
+#endif
 
 #include "renderthread/RenderThread.h"
@@ -51,6 +53,7 @@ AutoBackendTextureRelease::AutoBackendTextureRelease(GrDirectContext* context,
                                                              createProtectedImage,
                                                              backendFormat,
                                                              false);
+#if !defined(__APPLE__)
     } else if (backend == GrBackendApi::kVulkan) {
         backendFormat =
                 GrAHardwareBufferUtils::GetVulkanBackendFormat(context,
@@ -70,3 +73,4 @@ AutoBackendTextureRelease::AutoBackendTextureRelease(GrDirectContext* context,
                                                                  backendFormat,
                                                                  false);
+#endif
     } else {
         LOG_ALWAYS_FATAL("Unexpected backend %d", backend);
diff --git a/AutoBackendTextureRelease.cpp b/AutoBackendTextureRelease.cpp
--- a/AutoBackendTextureRelease.cpp
+++ b/AutoBackendTextureRelease.cpp
@@ -131,5 +135,8 @@ void AutoBackendTextureRelease::releaseQueueOwnership(GrDirectContext* context) {
     if (!context) {
         return;
     }
+#if defined(__APPLE__)
+    LOG_ALWAYS_FATAL("Vulkan queue ownership is unavailable on Darwin");
+#else
 
     if (!RenderThread::isCurrent()) {
@@ -158,3 +165,4 @@ void AutoBackendTextureRelease::releaseQueueOwnership(GrDirectContext* context) {
         context->setBackendTextureState(mBackendTexture, newState, nullptr, releaseProc, this);
     }
+#endif
 }
