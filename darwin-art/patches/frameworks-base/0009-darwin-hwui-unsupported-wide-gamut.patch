diff --git a/renderthread/EglManager.cpp b/renderthread/EglManager.cpp
--- a/renderthread/EglManager.cpp
+++ b/renderthread/EglManager.cpp
@@ -313,4 +313,10 @@ void EglManager::initExtensions() {
     EglExtensions.glColorSpace = extensions.has("EGL_KHR_gl_colorspace");
     EglExtensions.noConfigContext = extensions.has("EGL_KHR_no_config_context");
     EglExtensions.pixelFormatFloat = extensions.has("EGL_EXT_pixel_format_float");
+#if defined(__APPLE__)
+    // Darwin's Metal/IOSurface backend exposes RGBA_8888 only. Keep the
+    // Android capability bit honest even if an underlying EGL provider
+    // advertises an FP16 extension.
+    EglExtensions.pixelFormatFloat = false;
+#endif
     EglExtensions.scRGB = extensions.has("EGL_EXT_gl_colorspace_scrgb");
