[package]
name = "bionic-formatted-stdio-facade"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[dependencies]
bionic-stdio-facade = { path = "../bionic-stdio-facade" }
darwin-art-elf-loader = { path = "../../crates/darwin-art-elf-loader" }

[workspace]
